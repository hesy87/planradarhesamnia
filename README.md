# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

# CLONE RUN APP
```bash
npm run dev
```
```bash
https://github.com/hesy87/planradarhesamnia.git
```

# Project Title
PlanRadar SaaS application

# Dependencies used in App

tailwindcss // A utility-first CSS framework
sass // CSS extension language
Vite // build tool
daisyui // adds component class names to Tailwind CSS
@hookform/resolvers // This function allows you to use any external validation library
react-hook-form // create forms
zod // form validation library
@reduxjs/toolkit // standard way to write Redux logic
react-redux // state management
react-router-dom // client side routing


